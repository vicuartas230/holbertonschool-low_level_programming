#include <stdio.h>

/*
 *This program prints all the numbers of base 16
 */

int main(void)
{
	for (int a = 48; a <= 57; a++)
	{
		putchar(a);
	}
	for (int b = 97; b <= 102; b++)
	{
		putchar(b);
	}
	return (0);
}
