#include <stdio.h>

/*
 *This program prints all possible combinations of single
 *digit-numbers
 */

int main(void)
{
	for (int a = 48; a <= 57; a++)
	{
		putchar(a);
		putchar(44);
		putchar(32);
	}
	return (0);
}
